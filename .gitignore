# Binary
terraform-provider-statusharbor

# Build artifacts
dist/
bin/

# IDE / editor
.vscode/
.idea/
*.swp
.DS_Store

# Acceptance-test scratch
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Locally-generated GPG key + passphrase. NEVER commit. The
# `gh secret set` invocation pushes these into GitHub repo secrets;
# the local copies stay on this machine for emergency recovery.
.secrets/

# Local override files
*.tfvars
!examples/**/*.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
